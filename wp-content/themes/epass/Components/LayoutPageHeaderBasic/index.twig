<flynt-component name="LayoutPageHeaderBasic" class="componentSpacing {{largeHeader ? 'lg'}}">
    <div class="container">
        <div class="content">
            <h1 class="{{ fn('is_front_page') ? 'h0'}}">{{pageTitle ? pageTitle : post.title}}</h1>
        </div>
    </div>
</flynt-component>
